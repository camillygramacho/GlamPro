spring:
  application:
    name: register-client
  datasource:
    url: jdbc:postgresql://localhost:5432/granprodb
    username: postgres
    password: 123456
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  mail:
    host: smtp.gmail.com
    port: 587
    username: glamprosalao@gmail.com
    password: jtzcublddeuolgvv
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  servlet:
    context-path: /glam-pro
